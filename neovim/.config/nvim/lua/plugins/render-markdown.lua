return {
  {
    "MeanderingProgrammer/render-markdown.nvim",
    opts = {
      html = {
        -- default conceals <!-- --> comments entirely; anti-conceal only
        -- reveals them on the cursor line
        comment = { conceal = false },
      },
    },
  },
}
